# Файл robots.txt для сайта vavilon.org
# Последнее обновление: 2023-11-15

# Основной User-agent - правила для всех поисковых роботов
User-agent: *
Allow: /                  # Разрешаем индексацию всего сайта по умолчанию
Disallow: /private/       # Закрываем доступ к приватным разделам
Disallow: /tmp/           # Временные файлы
Disallow: /search?        # Динамические поисковые запросы
Disallow: /admin/         # Админ-панель
Disallow: /api/           # API-эндпоинты
Disallow: /user/profile/  # Личные профили пользователей
Crawl-delay: 2            # Задержка между запросами (2 сек)

# Особые правила для Google
User-agent: Googlebot
Allow: /images/           # Разрешаем индексировать изображения
Disallow: /pdfs/          # Но запрещаем PDF-файлы
Crawl-delay: 1            # Меньшая задержка для Google

# Правила для Яндекс
User-agent: Yandex
Disallow: /cgi-bin/
Disallow: /logs/
Clean-param: ref /search/ # Игнорировать параметр ref в URL /search/

# Правила для систем мониторинга
User-agent: Pingdom
Disallow: /

# Sitemap (рекомендуется всегда указывать)
Sitemap: https://vavilon.org/sitemap.xml
Sitemap: https://vavilon.org/sitemap-images.xml

# Дополнительные директивы
Host: vavilon.org          # Основное зеркало сайта (для Яндекса)